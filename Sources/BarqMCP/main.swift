import Foundation

// barq-mcp — stdio MCP server that proxies to the Barq app bridge.
// Placeholder entry point; real implementation follows.
FileHandle.standardError.write("barq-mcp starting\n".data(using: .utf8)!)
